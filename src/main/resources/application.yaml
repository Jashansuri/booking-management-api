spring:
  application:
    name: booking-management-api
  jackson:
    default-property-inclusion: non_null

server:
  compression:
    enabled: true
    mime-types: application/json
  port: 8080

api:
  version: v1


restaurant-deals:
  url: "https://eccdn.com.au/misc/challengedata.json"
  fallback-resource: "data/challengeData.json"

logging:
  level:
    root: INFO
    com.ec.bookingmanagementapi: DEBUG

region: LOCAL
